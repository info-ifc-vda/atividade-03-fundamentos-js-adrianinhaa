const v = 30;
const d = 25;
const p = "roupa";

//console.log (v - d*0.1);

function desconto() {
    const desc = (v* d *0.01);
    console.log("O valor total é " + (v - desc)); 
    //return desconto;
}

desconto();

